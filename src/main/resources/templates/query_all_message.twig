{
  "version": true,
  "_source": [ "logsource","logtime","message"],
  "query": {
    "bool": {
      "must": [
        {
          "query_string": {
            "analyze_wildcard": true,
            "query": "*{{message}}*"
          }
        },
          {
          "range": {
            "logtime.keyword": {
              "gte": "{{starttime}}",
              "lt": "{{endtime}}"
            }
          }
        }
      ],
      "must_not": []
    }
  },
   "sort": { "logtime.keyword": { "order": "asc" } }
}