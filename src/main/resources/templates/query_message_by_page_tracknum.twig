{ 
 "version": true,
  "_source": [ "logsource","logtime", "{{tracknumtype}}" ,"message"],
  "query": {
    "bool": {
      "must": [
      {
          "terms": {
            "{{tracknumtype}}": [
              {{pagetracknum}}
            ]
          }
        },
       {
          "exists": {
               "field": "{{tracknumtype}}"
             }
        },
          {
          "range": {
            "logtime.keyword": {
              "gte": "{{starttime}}",
              "lt": "{{endtime}}"
            }
          }
        }
      ],
      "must_not": []
    }
  },
   "sort": { "logtime.keyword": { "order": "asc" } }
}