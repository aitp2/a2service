{
  "version": true,
  "_source": [ "logtime", "module","moduleEntryId", "moduleEntryTitle" ,"operator","operatorTime","nickname","sex"],
  "query": {
    "bool": {
      "must": [
        {
          "query_string": {
            "analyze_wildcard": true,
            "query": "{{query}}"
          }
        },
          {
          "range": {
            "logtime.keyword": {
              "gte": "{{starttime}}",
              "lt": "{{endtime}}"
            }
          }
        }
      ],
      "must_not": []
    }
  },
   "sort": { "logtime.keyword": { "order": "desc" } }
}